<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@android:color/holo_green_light">

    <TextView
        android:id="@+id/test_channel_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Channel Fragment"/>

    <Button
        android:id="@+id/test_music"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/test_channel_text"
        android:text="Play Test Episode"
        android:backgroundTint="@color/cardview_light_background"/>

    <Button
        android:id="@+id/test_channel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/test_music"
        android:text="Play Test Channel"
        android:backgroundTint="@color/cardview_light_background"/>

</android.support.constraint.ConstraintLayout>